{
  "scripts": {
    "dev": "nodemon -r dotenv/config src/main.js",
    "debug": "set DEBUG=express:* & nodemon src/main.js",
    "start": "node src/main.js",
    "live": "node reload/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "mg": "typeorm migration:generate -o -p -d src/db/connection.js"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.16",
    "ws": "^8.6.0"
  },
  "dependencies": {
    "express": "^4.18.1",
    "pug": "^3.0.2"
  }
}
